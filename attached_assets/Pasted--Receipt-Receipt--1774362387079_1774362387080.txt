现在进入第十二轮：Receipt / 调用记录最小闭环规划。

全程中文回复。先输出计划，不要直接改代码。

【本轮目标】
先不要直接做链上收据。
本轮只做“Receipt / 调用记录最小闭环规划”，把当前项目里与 receipt、call log、请求记录、结果沉淀相关的能力全面摸清楚，并给出一个低成本、最小可落地的方案，明确：

- 当前已经有什么数据模型
- 当前哪些地方已经在消费 receipt 数据
- 当前哪些 receipt 还是 Preview / 占位
- 最小可行的“非链上 receipt”能不能先做起来
- 哪些字段未来可自然升级到链上 receipt

【范围与成本约束】
1. 先做检查和规划，不要直接改代码。
2. 不新增任何依赖。
3. 不修改数据库 schema、合约、部署结构。
4. 不修改首页、Docs、Market、Playground、Dashboard 的现有业务逻辑。
5. 不要直接实现链上 receipt。
6. 不要直接接入真实支付结算。
7. 不要直接做复杂审计日志系统。
8. 目标是“低成本最小 receipt 闭环方案”。

【先检查再回答】
请先全面检查当前项目里与 receipt / 调用记录有关的实现，并告诉我：

1. 当前哪些文件和 receipt / call record 有关
2. 当前是否已经有：
   - receipt 数据表 / schema
   - receipt API
   - receipt hook
   - Dashboard receipt 列表
   - Playground / quote 成功后的记录逻辑
3. 当前 receipt 数据哪些是真实来源，哪些还是 Preview
4. 当前最影响“调用记录真实性”的 5 个问题是什么
5. 如果现在要做最小闭环，最推荐从哪里下手

【本轮需要你输出的规划内容】
请给出一个“最小 receipt 闭环方案”，至少包含以下内容：

一、当前状态总结
- 目前 receipt 相关真实做到哪一步
- 哪些页面已经消费 receipt
- 哪些地方只是 UI 占位或 Preview

二、最小版 receipt 目标
只规划最小闭环，不扩张：
- 用户完成一次 quote / 调用动作后
- 系统可以留下最小的一条真实记录
- 这条记录可以被 Dashboard 读取
- 但不要求链上存储
- 不要求支付完成
- 不要求真实结算

三、建议方案
请明确说明：
1. 是否复用现有 receipt 数据结构
2. 是否必须新增后端接口
3. 是否必须新增前端写入逻辑
4. 哪个入口最适合先触发记录：
   - quote 成功后
   - Playground 手动点击后
   - Market 中某个动作后
5. 为什么这是当前最省成本的方案

四、明确“现在不做什么”
请列清楚这轮先不做：
- 不做链上 receipt
- 不做支付证明
- 不做 tx hash 真值写入（除非当前已有）
- 不做复杂审计事件流
- 不做多步骤执行日志
- 不做队列 / worker
- 不做外部日志服务

五、后续分阶段建议
请把 receipt 相关工作拆成 3 个阶段：
- Phase 1：最小本地 / 数据库 receipt 沉淀
- Phase 2：与 quote / playground 结果绑定
- Phase 3：为链上 receipt 预留映射
每个阶段只写最核心目标，不要扩成大系统。

【特别关注的字段】
请重点核查并说明当前是否已有、未来是否可复用这些字段：
- serviceId
- payer
- provider
- token
- amount / price
- requestHash
- responseHash
- success
- latencyMs
- txHash
- createdAt

请明确区分：
- 现在已有并真实可写
- 现在已有但还没被真实写入
- 现在没有，不应伪造

【输出要求】
请用中文按下面格式输出：

1. 当前 receipt 相关文件清单
2. 当前真实已有的 receipt 能力
3. 当前只是 Preview / 占位的部分
4. 最影响真实性的 5 个问题
5. 最小 receipt 闭环方案
6. 是否需要新增后端接口
7. 这轮明确不做什么
8. 三阶段推进建议
9. 如果下一轮正式开始做最小 receipt 闭环，最小会改哪些文件

等我确认后，再决定是否进入正式开发。