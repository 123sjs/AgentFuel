现在进入第四轮：Services / Service Market 页面专业化升级。

全程中文回复。先输出计划，不要直接改代码。

【本轮目标】
把 /services 页面升级成一个专业的 AI Agent 服务市场页，让它和首页、Dashboard 的品牌风格统一，并且为后续真实接入：
- 服务浏览
- 搜索筛选
- 服务上架
- 获取报价
预留结构。

【成本与范围约束】
1. 不新增任何依赖。
2. 不修改支付逻辑、数据库结构、合约、部署结构。
3. 不新增后端 API。
4. 不新增页面路由。
5. 只允许修改 Services 页面相关文件和必要的局部样式/组件。
6. 不要顺手改首页、Dashboard、支付流程。
7. 所有 mock 内容必须明确标注 Preview / Example，不允许伪装成真实业务数据。

【先检查再回答】
请先检查当前 /services 的真实用途和相关文件，告诉我：
1. /services 对应的是哪个页面文件
2. 当前页面已有哪些功能
3. 当前哪些数据来自真实 hook / API
4. 当前哪些内容是静态占位或容易误导的
5. 当前最影响专业感的 5 个问题是什么

【视觉要求】
延续当前首页和 Dashboard 的品牌系统：
- 深色
- 专业
- 克制
- 基础设施感
- 黑 / 深蓝 / BSC 金
- 轻描边
- 轻毛玻璃
- 不做花哨大动画
- 不要像模板站
- 不要像 meme coin 官网

【页面结构要求】
请按下面结构设计 Services 页面：

1. 顶部区域
- 标题：Service Market
- 副标题：Discover agent-powered services, APIs and MCP endpoints on BSC.
- 右侧按钮：
  - Explore Pricing（可做占位按钮）
  - List Service（如果当前已有上架入口，保留并专业化；如果未真正接通，必须明确标注 Preview）

2. 搜索与筛选区
保留当前真实可用的搜索/筛选逻辑，视觉升级：
- 搜索框
- Token / Status / Category 等筛选（按当前已有能力决定）
- 结果数量
- 不能伪造筛选能力，已有什么就做什么

3. 服务卡片或列表区
每个服务至少展示：
- Service Name
- Description
- Endpoint / Provider
- Price
- Payment Token
- Stake Required（没有真实字段就显示 --）
- Status
- CTA：Get Quote / View Service

要求：
- 有真实数据时显示真实数据
- 没有真实数据时才显示 Preview / Example 卡片
- 不允许真实数据与 Preview 卡片混杂误导
- Preview 卡片必须有明显标识

4. Featured / Trusted 区块（仅在当前数据结构支持时）
- 只有在当前代码里有可靠字段可区分时才做
- 如果没有可靠字段，不要硬做“Featured”或“Trusted”

5. 服务上架入口区
如果当前已有 List Service modal / 表单：
- 只做专业化 UI 升级
- 不改核心提交流程
- 不改后端逻辑
- 如果功能未完全接通，明确标注 Preview

如果当前没有真正可用的上架流程：
- 做一个占位式 CTA 区块
- 文案说明“Service listing flow is being refined”
- 不伪装成完全可用

6. 空状态 / 引导区
当没有真实服务数据时：
- 显示专业空状态
- 引导用户：
  - Connect wallet
  - Explore service listing
  - Prepare endpoint metadata
  - Join early providers

【数据与语义限制】
1. 不允许出现假交易量、假调用次数、假收入。
2. 不允许出现未确认的链上地址。
3. 不允许把未知单位的字段强行写成 USD / USDT / BNB。
4. 不允许把未接通功能包装成正式可用。
5. 任何 Preview / Example 内容必须明显标识。

【交互要求】
1. 保留当前真实可用的搜索、过滤、按钮逻辑。
2. 只升级视觉和信息层级，不乱改业务行为。
3. CTA 文案要准确：
   - 真能用的入口，正常展示
   - 还没完全接通的入口，标注 Preview / Coming soon
4. 移动端可读，卡片不能挤爆。

【执行前必须先输出】
请先用中文输出：
1. 本次会修改哪些文件
2. 哪些旧结构和逻辑会保留
3. 哪些区块会重做
4. 哪些数据会显示真实值，哪些只会显示 Preview
5. 如何避免真实数据和 Preview 内容混杂误导
6. 完成后的验收标准

等我确认后再开始改代码。