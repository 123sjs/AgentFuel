```txt id="a4t7m2" 现在进入第八轮：Quote 接口真实化 + /api/health 补齐。 全程中文回复。先输出计划，不要直接改代码。 【本轮目标】 把当前报价流程从“固定返回占位数据”升级为“基于真实服务数据返回 quote”，并补一个最基础的健康检查接口，确保： - /api/quote 会校验 serviceId - /api/quote 会读取真实服务数据 - inactive 服务不能正常报价 - quote 返回的 price / token / stakeRequired 来自真实服务记录 - /api/health 返回基础健康状态，便于后续部署检查 【成本与范围约束】 1. 不新增任何依赖。 2. 不修改数据库 schema、合约、部署结构。 3. 不新增复杂服务，不拆新后端。 4. 只允许修改 quote / health 相关后端文件，以及必要的最小前端适配文件。 5. 不要顺手改首页、Dashboard、Docs。 6. 不要把“真实报价”误写成“支付已完成”或“链上已结算”。 7. 不要接入真实链上支付，不要接入真实钱包签名。 8. 保持当前架构最小改动。 【先检查再回答】 请先检查当前 quote 和 health 相关实现，告诉我： 1. 当前 /api/quote 对应哪些文件 2. 当前 /api/quote 的请求参数有哪些 3. 当前 /api/quote 返回值哪些是固定写死的 4. 当前是否已经能访问服务表 / 数据库 / hook 5. 当前是否已有 /api/health，如果没有，最小实现方案是什么 6. 当前最影响“报价真实性”的 5 个问题是什么 【本轮必须实现的内容】 请按下面目标实现： 1. /api/quote 校验 serviceId - 如果缺少 serviceId，返回明确错误 - 如果 serviceId 不存在，返回明确错误 - 如果 serviceId 对应服务不存在，不能再返回默认报价 - 错误返回要可读，不要只丢模糊异常 2. /api/quote 读取真实服务数据 从当前真实服务数据源中读取至少这些字段： - id - name - price - currency - stakeRequired - active 然后 quote 返回中： - price 使用真实 service.price - token 使用真实 service.currency - stakeRequired 使用真实 service.stakeRequired - serviceId 使用真实匹配到的 serviceId 3. inactive 服务不能正常报价 如果服务 active=false： - 不返回正常 quote - 返回明确状态和错误信息 - 前端能区分这是 inactive，而不是普通报错 4. paymentHeader 绑定真实 quote 即使当前 paymentHeader 仍然是占位式 header，也必须和真实 quote 绑定，至少包含这些语义信息： - serviceId - price - token - timestamp 或 nonce 不要再返回与服务无关的固定字符串。 5. /api/health 最小实现 新增一个最基础健康检查接口，至少返回： - ok - service 名称或 app 名称 - 当前时间 - 基础说明 不要过度设计，不要接复杂监控。 【前端最小适配要求】 如果 Playground 当前需要适配新的错误返回或 inactive 状态： - 只做最小改动 - 让错误信息更准确显示 - 不要重做整个 Playground - 不要伪造成功状态 【返回与语义要求】 1. quote 成功 ≠ 已支付 2. quote 成功 ≠ 已执行服务 3. quote 成功 ≠ 已记录收据 4. inactive 服务 ≠ 系统故障 5. serviceId 不存在 ≠ 默认服务 【API 设计要求】 请优先保持当前已有接口结构，最小化变更。 如果必须调整返回格式，请说明： - 旧格式是什么 - 新格式是什么 - 为什么必须调整 - 哪个前端文件需要最小适配 【执行前必须先输出】 请先用中文输出： 1. 本次会修改哪些文件 2. 哪些旧逻辑会保留 3. /api/quote 将如何从“固定返回”改为“真实服务驱动” 4. serviceId 缺失 / 不存在 / inactive 三种情况会如何返回 5. paymentHeader 将如何与真实 quote 绑定 6. /api/health 会返回什么 7. 哪些前端文件如果需要适配，会做哪些最小改动 8. 完成后的验收标准 等我确认后再开始改代码。